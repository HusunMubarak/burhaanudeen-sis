# PostgreSQL (default — run `docker compose up -d` for local dev, see README)
DATABASE_URL=postgresql://burhaanudeen:burhaanudeen@localhost:5432/burhaanudeen_sis

# NextAuth
# Generate a real secret with: openssl rand -base64 32
AUTH_SECRET="replace-with-a-long-random-string"
NEXTAUTH_URL="http://localhost:3000"
